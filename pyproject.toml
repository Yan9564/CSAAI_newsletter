[project]
name = "csaai-newsletter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["PyYAML>=6.0", "feedparser>=6.0.11", "openai>=1.40.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
